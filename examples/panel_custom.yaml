# Legacy: manual panel_custom setup (without the HACS integration)
#
# Prefer the HACS Integration flow instead — it registers the panel automatically.
# Only use this if you install dashboard.js manually under /config/www/community/.

panel_custom:
  - name: react-dashboard-studio-panel
    sidebar_title: Dashboard Studio
    sidebar_icon: mdi:react
    url_path: react-dashboard-studio
    module_url: /local/community/react-dashboard-studio/dashboard.js
